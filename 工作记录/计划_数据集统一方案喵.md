# 计划：参照 EarthDial 架构统一遥感多任务数据集

## 背景

我们有 7 个遥感数据集（总计约 480GB，位于 `F:\挑战杯数据集\`，通过软链接 `d:\my_paper\揭榜挂帅\dataset\` 访问），格式各异（JSON、LLaVA conversation、HuggingFace Arrow），语言混用（中/英），任务类型多样（变化描述、图像描述、视觉定位、VQA、变化检测）。

**核心问题**：这些异构数据集无法联合训练。需要参照 EarthDial 的设计理念，将其统一为一种标准格式，以便后续一起训练。

**目标**：写一套 Python 脚本，实现：
1. 将每个数据集转为统一 JSON 格式，每条样本带 EarthDial 风格的 token 前缀
2. 对大图做 448×448 的 tile 切分（参照 EarthDial Adaptive High Resolution）
3. 合并产出统一的 train/val/test 划分

## 产出物结构

```
d:\my_paper\揭榜挂帅\
├── scripts/                          ★ 新建目录
│   ├── config.py                     # 全局配置（路径、token映射、切图参数）
│   ├── utils/
│   │   ├── tiler.py                  # 448×448 切图引擎
│   │   ├── token_prefix.py           # 生成 [传感器 分辨率 波段][任务] token
│   │   └── image_utils.py            # 图片加载/存储/转换工具
│   ├── convert_levir_mci.py          # 变化描述 + 变化检测
│   ├── convert_levir_cc.py           # 变化描述（A/B 配对图）
│   ├── convert_vrsbench.py           # Caption + Refer + VQA（LLaVA格式）
│   ├── convert_xlrs_lite.py          # 复杂推理VQA（Arrow内嵌图片）
│   ├── convert_xlrs_caption_zh.py    # 中文Caption（Arrow格式）
│   ├── convert_xlrs_vg.py            # 视觉定位中英文（Arrow格式）
│   ├── convert_mme_realworld.py      # 多选VQA（仅遥感子集）
│   ├── merge_datasets.py             # 合并所有 → 统一 train/val/test
│   ├── validate.py                   # Schema 校验 + 统计信息
│   └── run_all.py                    # 一键运行脚本
└── dataset/
    └── unified/                      ★ 生成的输出
        ├── train.json
        ├── val.json
        └── test.json
```

## 统一 JSON Schema

沿用 LLaVA conversation 格式（兼容 LLaMA-Factory 等训练框架），扩展 EarthDial 元数据字段：

```json
{
  "id": "数据集名/划分/唯一ID",
  "images": ["相对路径/A图.png", "相对路径/B图.png"],
  "conversations": [
    {
      "from": "human",
      "value": "<image>\n[optical 256 rgb][changecaption] 描述两张图中发生了什么变化。"
    },
    {
      "from": "gpt",
      "value": "新建了一栋建筑..."
    }
  ],
  "task": "changecaption",      // 任务类型
  "sensor": "optical",          // 传感器
  "resolution": 256,            // 图像分辨率（取大边）
  "bands": "rgb",               // 波段
  "split": "train",
  "language": "zh",
  "source": "levir_mci"         // 来源数据集
}
```

**核心设计**：
- `images` 永远是列表 — 单图任务一个元素，双时相变化描述两个元素（A图/B图）
- token 前缀 `[传感器 分辨率 波段][任务]` 直接写入 human 的对话内容中
- 所有图片路径为相对路径
- 定位任务的坐标统一归一化到 [0,1]，存储在 gpt 回复中

## EarthDial Token 前缀体系

由 `token_prefix.py` 实现：

| 参数 | 取值 | 生成的 Token |
|------|------|-------------|
| 传感器 | optical, sar, multispectral | `[optical]`, `[sar]`, `[multi]` |
| 分辨率 | 256, 512, 1024, 4096, 7360, 11500... | `[256]`, `[4096]`... |
| 波段 | rgb, grayscale, vh, vv | `[rgb]`, `[gray]`, `[vh]` |
| 任务 | changecaption, changedet, caption, refer, vqa, reason | `[changecaption]`, `[refer]`... |

**完整前缀示例**：
- LEVIR 变化描述：`[optical 256 rgb][changecaption]`
- VRSBench caption：`[optical 512 rgb][caption]`
- XLRS 视觉定位：`[optical 7360 rgb][refer]`

## 图像切图策略

由 `tiler.py` 实现：
- Tile 尺寸：448×448，stride=448（无重叠，与 EarthDial 论文一致）
- 短边 ≤448px → 不切（LEVIR 256×256、VRSBench 512×512 都在合理范围内，不切）
- 超过 448px → 网格切分，保存到 `dataset/unified/tiles/{数据集}/{原名}_r{行}_c{列}.jpg`
- JPEG 质量 90，平衡磁盘占用和画质

**需要切图的数据集**：
| 数据集 | 典型分辨率 | 每图 tile 数 |
|--------|-----------|-------------|
| XLRS-lite | 4096×4096 | ~81 |
| XLRS caption_zh | 最大 4096×4096 | ≤81 |
| XLRS VG | 最大 10000×10000 | ≤529 |
| MME-RealWorld RS | 最大 11500×7500 | ≤429 |

## 逐数据集转换方案

### 1. LEVIR-MCI（变化描述 + 变化检测）
- **输入**：`LevirCCcaptions.json` + 合并的A/B单张PNG + 变化mask
- **任务**：`[changecaption]` + `[changedet]`
- **处理**：`sentences[]` 中每条句子 → 一条 conversation。`changeflag=0` → 回复"无变化"。`changeflag=1` → 回复具体变化描述。同时生成变化检测条目（回复为 change / no change）
- **图片**：256×256 → 不切
- **产出**：~5万条 conversation

### 2. Levir-CC（变化描述，A/B 分离）
- **输入**：同 LEVIR-MCI 的 JSON + A/B 分目录存放的图片对
- **任务**：`[changecaption]`
- **处理**：将 JSON 条目与实际存在的 A/B 文件配对，每条句子生成一条 conversation。无 JSON 标注的图片跳过
- **图片**：`images: ["A/xxx.png", "B/xxx.png"]` — 两张独立文件
- **产出**：~4万条

### 3. VRSBench（Caption + Refer + VQA）
- **输入**：`VRSBench_train.json`（LLaVA 格式，14.2万条） + 逐图标注 JSON
- **任务**：`[caption]`、`[refer]`、`[vqa]`（从 human message 中的 `[task_type]` 标签提取）
- **处理**：解析现有 LLaVA 格式，提取任务标签，生成 EarthDial 前缀。Refer 坐标从 `{<x><y><w><h>}` 百分号格式归一化到 [0,1]。灰度图转 RGB
- **图片**：512×512 → 不切
- **产出**：~14.2万条

### 4. XLRS-Bench-lite（复杂推理VQA）
- **输入**：74 个 Arrow 分片，内嵌 JPEG 图片
- **任务**：`[reason]`（多选）
- **处理**：流式读取 Arrow 分片；提取内嵌 JPEG → 存盘；切 4096×4096 大图；将多选选项格式化写入 human message
- **图片**：全部 4096×4096 → 每张切 81 个 tile
- **产出**：~3200条

### 5. XLRS-Bench_caption_zh（中文 Caption）
- **输入**：22 个 Arrow 分片，中文描述
- **任务**：`[caption]`，语言：`"zh"`
- **处理**：流式读取 Arrow，提取图片，根据实际图片尺寸设定分辨率 token
- **图片**：最大 4096×4096 → 按需切
- **产出**：~2.2万条

### 6. XLRS-Bench Visual Grounding（视觉定位）
- **输入**：64 train + 64 test Arrow 分片
- **任务**：`[refer]`，语言：`"zh"`
- **处理**：流式读取 Arrow；bbox 已是归一化的 [xmin,ymin,xmax,ymax]；图片最大 10000×10000 → 大量切图
- **图片**：超大 → 大量 tile
- **产出**：~6.4万条（中文版）

### 7. MME-RealWorld（多选VQA，仅遥感子集）
- **输入**：`MME_RealWorld.json` + `MME_RealWorld_CN.json`
- **任务**：`[vqa]`（多选）
- **处理**：仅保留 `remote_sensing` 子集（~3700条英文）。将选项格式化写入 human message，答案写选项字母
- **图片**：最大 11500×7500 → 大量切图
- **产出**：~3700条（EN）+ 中文子集

## 合并策略

`merge_datasets.py`：
1. 收集所有数据集产出的 JSON → 按 split 拼接
2. 生成唯一 ID（`{来源}/{划分}/{序号}`）
3. 按任务类型做平衡采样（可选）：
   - 过采样数据量少的任务（如 changedet 只有 LEVIR-MCI 有）
   - 截断数据量过多的任务（如 VRSBench caption 占 ~7万条）
4. 输出统一的 `train.json`、`val.json`、`test.json`

## 实施顺序

| 阶段 | 脚本 | 原因 |
|------|------|------|
| **1** | `config.py`, `utils/token_prefix.py`, `utils/image_utils.py` | 基础设施，所有转换脚本依赖这些 |
| **2** | `utils/tiler.py` | 切图引擎，XLRS 和 MME 转换需要 |
| **3** | `convert_levir_mci.py`, `convert_levir_cc.py` | 最简单的数据集（标准JSON + 小图），先跑通流程 |
| **4** | `convert_vrsbench.py` | 条目最多（14.2万），覆盖3种任务类型，确立转换范本 |
| **5** | `convert_xlrs_lite.py`, `convert_xlrs_caption_zh.py` | 验证 Arrow 流式读取 + 切图流程 |
| **6** | `convert_xlrs_vg.py` | 最复杂（Arrow + 大图切分 + bbox坐标 + 数据量大） |
| **7** | `convert_mme_realworld.py` | 多选格式 + 超大图切分，优先级次之 |
| **8** | `merge_datasets.py`, `validate.py` | 合并 + 校验 + 统计 |
| **9** | `run_all.py` | 一键运行入口 |

## 验证方案

1. **逐数据集校验**：每个转换脚本内置验证：
   - 所有图片路径在磁盘上存在
   - JSON schema 字段完整
   - Token 前缀格式合法
   - Refer 坐标在 [0,1] 范围
   - 无空 conversation

2. **快速冒烟测试**：`python scripts/run_all.py --limit 10` 每个数据集只处理 10 条，验证全流程

3. **统计报告**：`validate.py` 输出：
   - 各任务/各语言/各数据集的条目数量
   - 图片分辨率分布
   - Tile 数量统计
   - 坐标格式校验

4. **人工抽查**：每个转换后数据集随机抽 5 条检查内容正确性

## 风险与对策

| 风险 | 对策 |
|------|------|
| Arrow 文件使用不同 IPC 版本 | 统一用 `pa.ipc.open_stream()`（流式格式），异常捕获 |
| 内嵌图片字节可能损坏 | decode 外包 try/except，日志记录并跳过坏条目 |
| Tile 存储量巨大（>500GB） | JPEG quality=90，仅 >448px 才切图，估约 ~200GB |
| Levir-CC JSON 与图片文件不匹配 | 交叉比对文件名，日志记录未匹配项 |
| 中文编码问题 | 全链路统一 `utf-8` |
| 120GB VG 数据集内存溢出 | Arrow 分片逐个流式处理，绝不全部加载到内存 |
